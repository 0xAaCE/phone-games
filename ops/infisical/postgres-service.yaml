apiVersion: v1
kind: Service
metadata:
  name: infisical-postgres
  namespace: infisical
  labels:
    app: infisical-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: infisical-postgres

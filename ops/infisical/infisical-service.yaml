apiVersion: v1
kind: Service
metadata:
  name: infisical
  namespace: infisical
  labels:
    app: infisical
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: infisical
